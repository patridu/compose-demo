# Compose Demo

Demonstrates some features of using Compose with OCI sources

- Defining ready-to-run container configurations
- Building on GitHub Actions and publishing to GHCR
- Referencing custom images with Dockerfiles
- Passing environment variables on the end host